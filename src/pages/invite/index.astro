---
import Layout from '../../layouts/Layout.astro';
import { getInvite } from '../../../backend/backend.mjs';
import Cardinvite from '../../components/cardinvite.astro';
const invites = await getInvite();
console.log(invites);
---

<Layout>
    <div class="bg-[rgb(238_242_255)]">
        <div class="container py-20 space-y-8">
            <h1 class="font-sans text-5xl text-pink-500 text-center">Invités</h1>

<div 
                                x-bind:class="view === 'grid' ? 'grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-6' : 'flex flex-col space-y-4 w-full max-w-3xl'"
                                class="w-full">
                               {invites && invites.map((invite) => (
    <Cardinvite {...invite} />
))}

                        </div>
        </div>
    </div>
</Layout>